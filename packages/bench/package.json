{
	"name": "@openjsxl/bench",
	"version": "0.0.0",
	"private": true,
	"description": "Private benchmark harness for openjsxl (never published; competitor libs live here so the shipped packages stay zero-dependency).",
	"type": "module",
	"license": "MIT",
	"scripts": {
		"bench": "node src/run.mjs"
	},
	"dependencies": {
		"openjsxl": "workspace:*"
	},
	"devDependencies": {
		"exceljs": "4.4.0",
		"xlsx": "0.18.5"
	}
}
