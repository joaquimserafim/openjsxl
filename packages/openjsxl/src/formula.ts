// openjsxl/formula — opt-in formula parser (M8). Mirrors `@openjsxl/core/formula`; importing this
// path loads the parser, while `import "openjsxl"` never does.

export * from "@openjsxl/core/formula";
