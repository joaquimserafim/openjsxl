node_modules/
dist/
coverage/
*.tsbuildinfo
.DS_Store
*.log
.idea/
.vscode/
!.vscode/extensions.json

# Local working status notes (not tracked)
progress.md

# Local operating contract for agents/humans (kept local, not published to the remote)
CLAUDE.md

# Local competitive-analysis snapshot (not tracked, never published)
COMPETITIVE-ANALYSIS.md

# Local-only test fixtures (e.g. differently-licensed real .xlsx files): usable for local
# verification, never committed or published. The README is tracked so the dir documents itself.
packages/fixtures/local/*
!packages/fixtures/local/README.md
